# What goes in `src/app/_components/`

### TL;DR

* **`src/app/_components/`** = Reusable components that ONLY belong to this route.

>Similar to `src/components/` but only components that pertain to this route.
